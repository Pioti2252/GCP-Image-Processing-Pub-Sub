apiVersion: v1
kind: Service
metadata:
  name: image-api
  namespace: image-processing
spec:
  type: ClusterIP
  selector:
    app: image-api
  ports:
    - name: http
      port: 80
      targetPort: http